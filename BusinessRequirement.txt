帮助大家找到志同道合的伙伴 移动端H5网页
1. 用户去添加标签，标签的分类
2. 主动搜索： 允许用户根据标签搜索其他用户
    1. Redis缓存 + 本地


3. 组队
    1. 创建队伍
    2. 加入队伍
    3. 根据标签查询队伍
    4. 邀请其他人

4. 允许用户去修改标签
5. 推荐
    1. 相似度计算算法 + 本地分布式计算


技术栈
前端
1. vue3 
2. Vant UI (基于vue的移动端组件库) （react 版zent）
3. Vite (打包工具)
4. nginx 单机部署

后端
1. java springboot 
2. spring mvc + mybatis + mybatis plus
3. mysql 数据库 
4. redis 缓存
5. swagger + knife4j 接口文档 

前端设计
导航条：展示当前页面名称
主页搜索框：搜索页
内容
tab 栏：
    主页（推荐页 + 广告）
        搜索框
        banner
        推荐信息流
    队伍页面
    用户页面（消息）


开发
很多页面复用组件，复用样式，重复写很麻烦，抽象一个通用的布局（layout）

数据库表设计
标签的分类 （要有哪些标签，怎么把标签进行分类）
标签表（分类表）
建议用标签不要用分类，更灵活
性别：男，女
方向： java, C++, go
正在学： spring
目标： 考研，求职，春招，秋招，社招，考公，竞赛（蓝桥杯，acm）
段位：初级，中级，高级，王者
身份：大一，大二，大三，大四，学生，待业，就业，研一，研二，研三，博一，博二
婚恋情况：单身，已婚，有对象，离异

用户自己定义标签？

字段 (field)：
id int 主键
标签名 varchar 非空
上传标签的用户 userID int
父标签id， parentid， int（分类）
是否为父标签 isParent, tinyint(0,1)
创建时间 createTime, dateTime
更新时间 updateTime, dateTime
是否删除 isDelete, tinyint(0,1)

怎么查询所有标签，并且把标签分好组？按父标签id 分组
根据父标签查询子标签？根据id查询？


sql 语句
DDL data definition language （建表）
DCL data control language （权限）
DQL data query language （查，where from）
DML data manipulation language (增删改)


修改用户表（分类表）
用户有哪些标签
根据自己的实际需求， 此处选择第一种
1. 直接在用户表补充tag 字段 ['java', 男] 存json 字符串
    优点： 查询方便，不用新建关联表，标签是用户的固有属性（除了该系统，其他系统可能要用到，标签是用户的固有属性） 节省开发成本
    查询用户列表，查关系表拿到这100个用户的所有标签，再根据标签id 去查标签表
    哪怕性能低，可以用缓存
    缺点：用户表多一列
2. 加一个关联表，记录用户和标签的关系
    关联表的应用场景：查询灵活，可以正查反查
    缺点：用户表多一列，多维护一个表
    大项目开发尽量减少关联查询，很影响扩展性和查询性能


开发后端接口
搜索标签

1. 允许用户传入多个标签，多个标签都存在才搜索出来 and
2. 允许用户传入多个标签，有任何一个标签存在就能搜索出来 or

两种方式
1.内存查询
2. sql 查询